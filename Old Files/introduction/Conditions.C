#include<stdio.h>
int main()
{
    //Adding to two numbers 
    int a=6;
    int b=5;
    int c;
    c=a+b;
    //Introducing the if condtion
    if(c==11){
        //if(condtion_here){The function if true}
        printf("Impressed by the addition? %d ",c);
    }else {
        //Executes when the if condtion comes out as true
            printf("Not impressed");        
    }
} 